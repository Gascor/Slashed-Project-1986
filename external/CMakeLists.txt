cmake_minimum_required(VERSION 3.20)

# Placeholder for third-party libraries.
# Populate with FetchContent declarations or add_subdirectory calls when the
# dependencies (GLFW, GLAD, stb, etc.) are added to the repository.
